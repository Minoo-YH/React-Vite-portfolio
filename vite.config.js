import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 3000, // Port for the frontend
    open: true, // Automatically open the browser when the server starts
  },
  plugins: [], // Your Vite plugins here, if needed
});


